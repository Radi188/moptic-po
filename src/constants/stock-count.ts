/** Route segment for the "every line in the count" entry of a stock count. */
export const ALL_CATEGORIES = 'all';

/** Counts are never paginated in the app — one page holds every line. */
export const ALL_ITEMS_PER_PAGE = 1000;
